<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tepia.reservoirthree"
    tools:overrideLibrary="com.inpor.fastmeetingcloud">

    <!--               tools:replace="android:icon,android:theme,android:allowBackup,android:label,android:name"

    -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name="com.tepia.reservoirthree.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo_white"
        android:label="@string/appkongname"
        android:roundIcon="@mipmap/logo_white"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme,android:name,android:label">

        <activity
            android:name="com.tepia.main.view.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>